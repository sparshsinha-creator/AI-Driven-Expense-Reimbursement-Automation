import claimsData from "../data/claims.json";

/**
 * Real output of this project's Phase 5 pipeline (data/final_decisions.json),
 * converted once into src/data/claims.json. Not mock data.
 */
export function useClaims() {
  return claimsData;
}
